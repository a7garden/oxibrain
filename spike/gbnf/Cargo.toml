[package]
name = "gbnf-spike"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
oxibrain-core = { path = "../../crates/oxibrain-core" }
llama-cpp-2 = "0.1"
encoding_rs = "0.8"
serde_json = "1"
anyhow = "1"

# Standalone — NOT a workspace member (spec §2: "throwaway, not a workspace member").
[workspace]

[profile.release]
opt-level = 3
