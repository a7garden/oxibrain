# Rust
/target
**/*.rs.bk

# Frontend (Phase 4)
node_modules/
dist/
# The console bundle is committed inside the oxibrain-mcp crate (ADR-008) so
# `cargo install oxibrain-cli` can serve it without Node, and `cargo package`
# embeds it in the published tarball. Any other dist/ stays ignored.
!crates/oxibrain-mcp/assets/dist/
.vite/

# OS / editor
.DS_Store

# OMP harness local state
/.omp/
/.oxicode/

# SDD workspace scratch
.superpowers/

# Git worktrees (isolated plan workspaces)
/.worktrees/

# proptest failure artifacts
proptest-regressions/
spike/gbnf/target/
spike/gbnf/Cargo.lock

# Local publish scripts (contain credentials — never commit)
.publish-remaining.sh
.publish.log
.publish*.sh
.release-prep-status.md
