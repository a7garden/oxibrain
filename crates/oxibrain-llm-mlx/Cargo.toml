[package]
name = "oxibrain-llm-mlx"
edition.workspace = true
version.workspace = true
license.workspace = true
description = "Native MLX inference adapter for oxibrain (Apple Silicon, in-process)"
repository.workspace = true
rust-version.workspace = true
readme = "../../README.md"
keywords = ["llm", "mlx", "apple-silicon", "metal"]
categories = ["api-bindings"]

[dependencies]
blake3.workspace = true
parking_lot.workspace = true
oxibrain-ports.workspace = true
async-trait.workspace = true
tokenizers.workspace = true
serde.workspace = true
serde_json.workspace = true
tokio.workspace = true
tracing.workspace = true

# MLX is Apple-only; the crate compiles to an empty surface elsewhere so
# `cargo build --workspace` and `--all-features` CI legs keep working on
# Linux runners.
[target.'cfg(target_vendor = "apple")'.dependencies]
mlx-rs = { workspace = true, features = ["safetensors"] }
half = "2"

[dev-dependencies]
ahash = "0.8"
tempfile.workspace = true
