# Phase M3 Handoff — Extraction & Evaluation → M4

> **Status:** M3 architecture complete. The LLM has arrived — non-determinism is
> contained, cached, and replayable. Eval harness with §14.2 gates passing.
> **Branch:** `main` (squash-merge flow)
> **Last commit:** `6b1b8bd` (feat(m3): eval harness + golden corpus + fast eval suite)
> **Predecessor:** M2→M3 handoff (`docs/superpowers/handoffs/2026-08-11-m2-to-m3.md`)
> **Tests:** 134 pass, 0 fail. Clippy clean. Standalone verified.

---

## 1. What Shipped (18 commits since M2 `2169590`)

| Commit | What |
|--------|------|
| `6b28c62` | docs(m3): extraction + evaluation design spec |
| `d448fe5` | docs(m3): extraction + evaluation implementation plan |
| `5ceac03` | fix(m3): community summaries ARE Derived episodes |
| `4b4aa22` | feat(m3): core extraction types, schema, validator, FakeLlmPort, BrainError |
| `6af0b38` | feat(m3): llm-http + job queue + cache + project_extraction + quarantine + consolidation |
| `21d20ba` | feat(m3): Brain facade — LLM port, ingest, extract, reextract |
| `1c8cf57` | feat(m3): reproject extraction replay + integration tests |
| `cc42e84` | feat(m3): consolidation + community summary facades |
| `6b1b8bd` | feat(m3): eval harness + golden corpus + fast eval suite |

---

## 2. M3 Exit Criteria Status

| Criterion (DESIGN §17) | Status |
|---|---|
| Job queue lifecycle | ✅ |
| LLM port + HTTP adapter (Anthropic + OpenAI) | ✅ |
| Registry-generated JSON Schema | ✅ |
| Forced structured output | ✅ |
| Validator (7 rules incl. verbatim surface gate) | ✅ |
| Repair loop (one retry, partial acceptance) | ✅ |
| Mention capture (verbatim + byte span) | ✅ |
| Extractor identity + re-extraction | ✅ |
| Reprojection determinism (extraction cache replay) | ✅ |
| Consolidation + community summaries (cached Derived) | ✅ |
| Confidence calibration types | ✅ |
| Golden corpus + eval harness (fast suite) | ✅ |
| §14.2 quality gates (tested with controlled fixtures) | ✅ |
| §13.2 budget numbers measured | ⚠️ bench compiles, numbers not yet recorded |
| CLI subcommands (extract/reextract/eval) | ⚠️ deferred to M4 CLI work |
| Real corpus + live provider test | ⚠️ requires API key |

---

## 3. Remaining Polish (non-blocking for M4 start)

1. **Run `cargo bench`** and record §13.2 numbers in DESIGN.md (mechanical).
2. **Add CLI subcommands**: `oxibrain extract`, `reextract`, `eval` (straightforward — wrap Brain facade methods).
3. **Grow golden corpus** to ~50 fixtures (currently 3) — add Korean fixtures.
4. **Full eval suite** with live provider — requires API key + network.
5. **`cargo deny check`** — verify no license issues with reqwest deps.

---

## 4. What M4 Needs from M3

M4 (Surfaces + Security) builds on M3's extraction pipeline:
- **MCP server**: exposes `ingest`, `extract`, `query`, `declare`, etc. over MCP.
  The Brain facade methods are the implementation — MCP just wraps them.
- **CLI full surface**: `oxibrain extract`, `reextract`, `eval` subcommands wrap Brain methods.
- **Sampling LlmPort**: M4's MCP sampling (§12.3) is an alternative LlmPort adapter.
  The extraction pipeline already accepts any `LlmPort` impl.
- **Spaces/scopes/tokens**: the extraction pipeline is already space-scoped.

---

End of handoff. M3 extraction pipeline is complete and tested. Start M4 from here.
